import { createGlobalStyle } from 'styled-components/native';

export const Fonts = createGlobalStyle`
  @font-face {
    font-family: 'Signika Negative';
    src: url(require('./SignikaNegative.ttf'));
  }
`;