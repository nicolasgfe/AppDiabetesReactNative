import FakeHintEntry from "../FakeModel/FakeHintEntry";

// Quando o banco ficar pronto, substituir FakeHintEntry
// Usar um modelo realm apropriado

export interface HintItemProps {
  hintEntry: FakeHintEntry
}
